[project]
name = "camera-core"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "hardware-core",
    "httpx>=0.27.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["camera_core"]
